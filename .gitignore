# Whitelist only app folders committed: client/, server/, and deploy glue at repo root.
/*
!.gitignore
!/.dockerignore
!/Dockerfile
!/client/
!/server/

# Must un-ignore all paths under allowed dirs (see gitignore(5) parent ignore rules).
!/client/**
!/server/**
